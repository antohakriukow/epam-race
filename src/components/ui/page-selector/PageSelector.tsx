import { FC } from 'react';

import styles from './page-selector.module.scss';

const PageSelector: FC = () => {
  return <div className={styles.container}>PageSelector</div>;
};
export default PageSelector;
